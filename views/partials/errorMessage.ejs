<% if (locals.errorMessage != null) { %>
    <%= errorMessage %>
<% } %>

<!-- allows it to render text -->
<!-- the if statement only allows the errorMessage to display when the error is not empty -->